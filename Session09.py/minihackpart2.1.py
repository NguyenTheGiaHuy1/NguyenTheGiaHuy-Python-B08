colors = ['red', 'blue', 'yellow', 'green', 'purple']

while True:
    for i in colors:
        print(i)

        from turtle import *
        shape("turtle")
        color(i)
        speed(-1)
        forward(10)
        
mainloop()
